node_modules/
dist/
mta_archives/
gen/
db.sqlite
db.sqlite-shm
db.sqlite-wal
*.mtar
.env
.env.local
*.log
coverage/
approuter/webapp/

frontend/test-results/
frontend/playwright-report/

# Playwright screenshots
frontend/e2e/screenshots/

# Mock fixtures — local dev only, not deployed to BTP.
test/fixtures/*.json

# Production seed CSVs are committed (shipped to HANA Cloud on BAS deploy).
# Regenerate them from SAP via: node scripts/generate-seed.mjs
